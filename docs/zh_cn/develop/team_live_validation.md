# 团队演出预览验证（2026-09-22）

## 范围

本次仅开放团队演出，协力演出 pending。保留自由／巡演配置与已有入口识别修复。团队使用游戏随机歌曲，不提供房型和选曲选项；SP 不存在时回退 EX，其他难度不替换。所有本次实机验收均使用 0 火、小偏差，复用现有谱面首键同步和持续校时播放。

匹配前实际校验中国服 734 首歌曲的 868 张 EX／SP 谱面。此项证明缓存完整性，不代表每首歌曲都经过实机演奏。

## 实机记录

| 场景 | 结果 | 本地证据 |
|---|---|---|
| 请求 SP，随机到无 SP 的「星の約束」 | 自动选择 EX，762 PERFECT、0 MISS，完成 1 次并返回主页 | `debug/chart_live/20260922-225324/report.json` |
| 等待期间被游戏退回团队首页 | 自动重新匹配，随后「Don't say "lazy"」SP，763 PERFECT、0 MISS；尝试 2 次，成功 1 次 | `debug/team-timeout-test/report.json` |
| 空私人房间等待 3 分钟 | 本地计时在 180.734 秒触发退出；首次未识别私人房间确认框而停止。补充识别后，单独恢复检查自动确认退出并返回菜单 | `debug/team-timeout-local-test/report.json`、`debug/team-timeout-recovery/recovered.png` |
| 预览包实际 Agent 入口，连续中断后完成 | 第一次在 110 连击时切后台，停止触控并自动重进；第二个公开房间在 180.688 秒未开演后自动退出，退避 6 秒重进；第三次「Don't say "lazy"」SP，763 PERFECT、0 MISS。尝试 3 次、成功 1 次，最终返回主页，任务成功 | `debug/team-package-validation/20260922-232505/report.json` |

上述第二项的游戏主动退出发生在本地超时之前，因此不能单独作为 180 秒策略的验收依据。第三项保留首次失败记录，不将分步恢复表述为首次整轮成功。

## 页面识别与回归

使用真实截图验证团队首页、随机选曲、最终确认、队伍匹配、私人房间退出确认、连接错误、二次中断确认；确认难度标签及 0 火预览。弹窗先于背景页识别，断线时选择“中断”并再次确认，避免继续已经转为单人的演出。结果见 `debug/online-frame-validation.txt`。

单元测试覆盖配置兼容、团队隐藏选曲、SP 回退、同名歌曲拒绝猜测、全池预缓存、联网不点击单机开演、各阶段中断不计数、180 秒跨页面累计、退避、用户停止、后台检测、弹窗优先及结算去重。完整测试结果保存在 `debug/team-regression-tests.txt`；界面与流程 schema 另行校验。

最终 142 项测试通过，界面与 pipeline schema 全部通过。预览包为 `dist/MaaBanG-v0.4.0-team-preview1-win-x64.zip`，入口 `MaaBanG.exe`，通过包内 Agent 注册握手及上述完整联网任务验证。打包前将实机日志移存到根目录 `debug/team-package-validation`，避免将测试日志、设备信息和临时通信配置带入分发包。包内保留已校验的谱面缓存，首次运行仍逐一校验再匹配。

未知歌曲或无法可靠确认的页面会停止并保留现场；当前事件未出现的维护／账号限制和不同活动结算布局未逐一实机触发。团队道具补火复用原有已验证实现，本次 0 火验收未消耗回复道具。预览包不自动发布为正式版本。

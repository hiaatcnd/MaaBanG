# 挖矿 preview4：从 1 级直接练到特训后满级

2026-09-23，MuMu `127.0.0.1:16416`。

## 实机结果

用户明确要求验证游戏内一次练习直接满级。三星“全心全意地营业中／羽泽鸫”起始 Lv.1/40；启用“自动特训”后显示 Lv.1/50。使用修改后的 `StoryMiningFlow.practice(row, 50)` 从未勾选状态完整执行：开启自动特训、推荐练习券、核对材料、提交一次、处理特训结果和练习页面，最终成员详情确认 Lv.50/50。

本次消耗 562 张单张练习券，以及确认窗中的 1000、300、3 特训材料。生产函数实测记录为 `debug/level50_inspect/level1_production_report.json`，起点 `level1_target.png`、推荐预览 `practice_1.png`、消费确认 `auto_training_1.png`、最终等级 `level1_to50_actual.png`。确认前只浏览和取消过一次预览，没有重复消费。

此前“盛夏的日光／今井莉莎”40→特训→50 的分段实测不作为此次从 1 级一次满级的证据。

## 修复与验证

- 先校验练习开关和允许星级，再启用自动特训，读取更新后的上限，最后检查故事等级资格和推荐用券。
- 自动特训确认窗独立识别，读取全部材料数量；不足或无法读取则取消，不提交。
- 消费确认只提交一次，随后处理插画和特训结果，读取实际等级验证成功。
- 更新任务和练习选项说明，明确会使用特训材料。
- 173 项回归测试通过，包括从 1/40 切换至 1/50 后可练习、排除星级不启用特训、材料不足不提交。

## 范围

此次验证直接调用生产练习函数，未重新运行整项成员扫描，也未再次消耗回忆故事解锁材料。当前已达旧上限、详情没有自动特训勾选入口的成员仍需先手动特训；该分支未改为自动操作。已打包 preview4，未发布 GitHub Release。

# 自由演出全曲识别核对（2026-09-23）

在 MuMu `127.0.0.1:16416` 的中国服实际页面，从「自由演出 → 所有」清空筛选后逐首点选，直到可见列表末尾。共采集 **736 首不同歌曲**，与修正后的 736 首可用目录完全对应，没有遗漏或重复 ID。没有点击选歌确认或开始演出，没有消耗火。

## 全量结果

| 核对对象 | 原逻辑通过 | 修正后采集记录复核 |
| --- | ---: | ---: |
| 选中卡片的歌名及必要的乐队消歧 | 708 / 736 | 736 / 736 |
| 下一首灰色小卡片文字 | 700 / 735 | 735 / 735 |

首首选中后连续两次读取一致才记入记录；掉点击会重试。到尾部时确认选中卡片未改变且下方不存在下一张卡片。第一次扫描的 68 首位置遇到掉点击，修正边界判断后从原位置续扫，记录未重置、未重复计数。

- [逐首核对清单](../../data/free_song_recognition_audit.csv)：歌曲 ID、目录歌名、乐队、实际 OCR、修正前结果及截图编号。
- 原始证据：`debug/free-song-audit/records.jsonl`、`frames/0001.jpg` 至 `frames/0736.jpg`、无损 `titles/*.png`。
- 原始扫描结论：`summary.json`；修正后的全量匹配结论：`verified_summary.json`。
- [列表末尾实拍](../../../debug/free-song-audit/frames/0736.jpg)。

## 修正

27 首选中歌名发生 OCR 漏识别，主要是引号、重复感叹号、音符和圆圈等特殊符号，以及相似日文字形。另补充列表小字独有的识别差异；所有新增别名均逐张查看截图后确认，并检查没有与其他歌曲名称冲突。自由演出、巡演及联网最终歌曲识别共用精确别名，未引入相似度猜歌。

正式选曲函数抽查时，还捕获《“Say cheese!!!!!”》在整块列表 OCR 中多出引号的情况，保留 `search_646_6.png` 及 `search_trace.log`。除精确别名外，将直／弯引号统一，并忽略边界处重复的引号，歌名正文仍完整匹配。全目录对比未产生新增名称冲突。

### Second to None：目录误排除

上游歌级信息标明已在中国服发布，但四个难度的中国服发布日期都是空值，旧生成器将其全部视为不可用，导致整首歌曲被排除。实际游戏中分别点击确认：

| 难度 | 实际等级 | 校验谱面音符数 |
| --- | ---: | ---: |
| EASY | 10 | 117 |
| NORMAL | 15 | 245 |
| HARD | 24 | 537 |
| EXPERT | 27 | 894 |

证据为 `debug/free-song-audit/second_to_none_*.png` 和 `second_to_none.json`。四份谱面均已下载并通过现有 `ChartStore` 的音符数、事件编译及手势范围校验。

增加明确的游戏实测记录 `agent/data/song_cn_verified_availability.json`，生成器只对这首歌的相同 ID、歌名、乐队及等级补正空日期造成的可用性缺失。不修改未知发布日期，不覆盖将来发布日期，不重新开放已下架歌曲。目录、界面候选和歌曲 CSV 已同步；与本次修正前目录对比，仅 ID 690 的歌曲数据改变。

## 验证边界

最终回归：**180 项测试通过**，包括全部实测文字对应歌曲 ID 的回归、精确别名冲突检查、未知标题拒绝、引号变化和目录补正边界。日志为 `debug/free-song-audit/regression.log`。

正式 `find_song()` 函数在游戏内逐项成功查找并选中以下 6 首，过程未点击选歌确认：

| ID | 歌曲 | 乐队 |
| --- | --- | --- |
| 170 | Jumpin' | Poppin'Party |
| 646 | “Say cheese!!!!!” | Afterglow |
| 524 | ふぁいぶスマイル◯オールインっ！ | Hello, Happy World! |
| 316 | オレンジ | Hello, Happy World! |
| 676 | オレンジ | Afterglow |
| 690 | Second to None | Roselia |

抽查证据为 `debug/free-song-audit/find_checks.json`、`find_checks.log` 及 `find_<ID>.png`；最终任务成功返回，随后清空了歌曲筛选。新增精确 OCR 别名共涉及 39 首、46 个实测文字变体。

本次全量覆盖的是自由演出里的选中卡片及相邻列表小卡片，并用保存的实际 OCR 记录逐项复核修正后的匹配。不是修正后再次逐首遍历游戏，也不是把 736 首逐首开演；全部歌曲的各难度及联网页面布局不属于本次全量覆盖。正式按乐队／等级查找流程另作代表歌曲抽查。

本次修正保留在工作区；尚未重新生成预览 ZIP，也未发布正式版本。

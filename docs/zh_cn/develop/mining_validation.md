# 挖矿验证记录

后续完整实机回归与成员故事修复见 [preview3 验证记录](mining_preview3_validation.md)。

日期：2026-09-23。设备：MuMu，`127.0.0.1:16416`，原始截图 2560×1440，框架识别 1280×720。

## 自动检查

- 全部 162 项单元测试通过，含真实星星截图、难度多选/全不选、最大次数、火数与火不足停止、练习星级/等级限制、材料图标数量、下一等级锁定和跨挑战恢复。
- Pipeline 与 interface schema 校验通过。
- Agent 握手通过，包含 `MineFullCombo`、`MineStories`、`MineChallenges`。
- `git diff --check` 通过。

## 实机验证

| 场景 | 结果 |
| --- | --- |
| 自由演出 EASY，极小偏差、0 火 | ときめきエクスペリエンス！130 PERFECT，其余判定为 0，选曲页复核为 AP。首次结算遇到 OCR 将“一览”识别为“ー览”，修正后从原现场完成结算回查 |
| 只勾选 NORMAL，最大 1 次、0 火 | 走り始めたばかりのキミに 205 PERFECT，其余判定为 0；报告确认 `star_after=full_combo`，完成 1 次后返回主页 |
| 主舞台 | 推荐编组、EXPERT 演奏、结算通过；根据实机补齐了结算角色对话与返回主页分支 |
| 特别舞台 | ReSonance 第 9 舞台 chAngE EXPERT，712 PERFECT，其余判定为 0；演出次数上限生效。随后独立验证恢复原挑战为第 10 舞台，确认第 9→10 的推进 |
| 区域道具 | 观察并实现“确认更换”及“已为当前编成乐队设置了最优区域道具”两步弹窗 |
| 特别舞台筛选 | 恢复“全部”乐队通过，避免历史筛选漏掉挑战 |
| 成员筛选 | 未读故事选择、全部属性/角色与动画条件恢复通过 |
| 成员星级 | 普通和特训后彩色星星的三星成员均识别为 3 星 |
| 故事阅读 | 已解锁故事，无语音下载、菜单、跳过确认、返回成员详情通过；本次为重读，未产生首次奖励 |
| 材料识别 | 三项材料分别读取为 1060131/1800、689868/1200、15689/300，未提交解锁 |

特别舞台返回主页会重置为主舞台，不能使用重新进入时默认选中的等级判断进度。实现已按舞台类型和挑战图标恢复原项目，且拒绝异常跨级；恢复逻辑在修正后单独实机验证。

## 尚未实际消耗验证的分支

材料解锁、练习券消耗、首次故事奖励，以及挖矿流程中的道具补火尚未在本次验证中实际执行。道具补火复用现有谱面演出流程；解锁和练习默认关闭。不会自动特训：等级上限低于故事要求时跳过。

本次未发布版本或更新已安装软件包。原工作区其他未提交改动保留。

## mining-preview2：长歌名开演校验修复

preview1 在 2026-09-23 01:53 的自由演出挖矿中，将歌曲 25 的准备页歌名
`キラキラだとか夢だとか ～Sing Girls～` 截成 `キラキラだとか夢だとか〜Sing Girl`，
因此开演前校验失败，未开始演奏。自由演出歌名识别区域从 x=220..660 扩大到
x=220..790，仍在自动演出按钮左侧；保留完整歌名和难度校验，错误信息增加实际识别值。

使用此次截图中不含账号信息的歌名/难度区域进行离线真实 OCR 回归：旧区域复现截断，
新区域通过；换成另一首歌或 EXPERT 仍被拒绝。完整 163 项测试通过。
